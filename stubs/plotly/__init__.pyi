"""Package marker for `plotly` stubs used by mypy.

Ensures `plotly` is recognized as a package so submodules like
`plotly.graph_objects` aren't treated as top-level modules.
"""

__all__: list[str] = []
